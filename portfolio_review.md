# ポートフォリオレビュー

ポートフォリオを確認しました。

## 総評

- 研修生の作成物としては完成度が高く、よくできています。

## 良かった点

- MVCやコンポーネントなど、責務の分離を意識したディレクトリ／ファイル構成ができている
- コメント（PHPDoc）がしっかりと記載できている
- CSRFトークンなど、セキュリティ面の対応についても学習し、実装しようとしている

## 改善点

### 優先度：大（本番運用上、問題となる内容）

#### 1. エラー表示がブラウザのコンソール上でそのまま見えてしまう

- **問題点**  
  エラー内容がブラウザ側のコンソールに直接表示されている。
- **改善案**  
  基本的にはブラウザ側のコンソールには詳細なエラーを表示せず、サーバー側のログ出力で確認できるようにする。

#### 2. いいね機能に脆弱性がある

- **問題点**  
  現状はフロントエンドから送信された値をそのまま利用しているため、送信側で値を改変することで、1日の上限などを回避できる状態になっている。
- **改善案**
  - バックエンド側でも値や条件を検証する
  - 日時についてはクライアントから送信された値を信用せず、サーバー時間を利用する
  - 1日の上限などの制約はバックエンド側で必ず判定する

#### 3. ギャラリーなどの画像サイズが過大

- **問題点**  
  大きな画像をそのまま表示しているため、ページ表示が重くなる可能性がある。
- **改善案**
  - サムネイルを生成する（幅800px程度を目安）
  - WebPなどの軽量な画像形式を利用する
  - `width` / `height` 属性を指定する
  - 用途に応じて画像サイズを制御する

#### 4. クラス名の誤り

- **問題点**  
  `allGalleryListPageController.php` 内のクラス名が `AllBlogListPageController` となっており、別クラスと重複している。
- **リスク**  
  将来的に重大なバグにつながる可能性が高い。
- **改善案**  
  ファイルの役割に対応した正しいクラス名へ修正する。

#### 5. `GamePageController::show()` のFilePath取得先が誤っている

- **問題点**  
  ゲーム用のファイルパス取得処理で `getBlogFilePath` が使用されている。
- **改善案**  
  `getGameFilePath` が正しいと思われるため、処理内容を確認したうえで修正する。

#### 6. HTML構造が崩れている箇所が散見される

例：

- `</main>` や `</div>` の閉じ忘れ
- `<span class="like_count">` に対応する閉じタグがなく、`<span>` になっている箇所がある

**改善案**  
HTML Validatorなどを利用し、タグの対応関係やHTML構造をチェックする。

#### 7. `.gitignore` のパターンと実ファイル名に齟齬がある

- **問題点**  
  `.gitignore` では `/sakuraDSN` となっている一方、実ファイル名は `sakuraDSN.php` となっている。
- **リスク**  
  このままではDB認証情報を含むファイルを誤ってGitへコミットする可能性があり、セキュリティ上危険。
- **改善案**  
  実際のファイル名と一致するように `.gitignore` を修正し、対象ファイルがGitの管理対象外になっていることを確認する。

### 優先度：中（現状の動作には問題ないが、将来的に問題となる可能性がある内容）

#### 1. パフォーマンスを意識できていない実装が散見される

##### 記事メタデータの取得

- 毎回すべてのファイルを走査する実装になっている。
- ファイル数が増加すると、パフォーマンスが低下するリスクが高い。

**改善案**  
キャッシュ、インデックス情報、DB管理など、全ファイル走査を避ける仕組みを検討する。

##### DB関連

- `getLikeCount` で毎回 `SELECT *` を実行して全件取得しているため、データ量が増えるほど処理が重くなる。
- 「いいね」1件につき1レコードを作成する設計の場合、利用者やアクセス数の増加に伴いレコード数が急増する。

**改善案**

- 件数だけが必要な場合は `COUNT` を利用する
- 必要なカラムのみ取得する
- インデックスを適切に設定する
- 「いいね」の保存方式について、想定データ量を踏まえて設計を検討する

#### 2. ルート判定が部分一致になっており不正確

現在の例：

```php
strpos($page, "blogList") !== false
```

**問題点**  
意図しない文字列まで一致する可能性がある。

**改善案**  
完全一致、または要件に応じた正規表現などを使用して、より正確に判定する。

#### 3. 命名規則の揺れやタイポがある

例：

- `topPageController` が小文字始まりになっている
- `createCookie` → `createCookie`

**改善案**  
クラス名・メソッド名・変数名などの命名規則をプロジェクト内で統一する。

### 優先度：小（不具合ではないが改善するとよい内容）

#### 1. READMEやdescriptionが提出向けになっていない

- **改善案**  
  他人に見てもらうことを意識し、最低限以下の内容をREADMEへ記載するとよい。
  - プロジェクトの概要・目的
  - 主な機能
  - 使用技術
  - 環境構築・起動方法
  - 工夫した点
  - ディレクトリ構成

#### 2. CSSの不備

以下のような記述に問題がある。

```css
url("/../../images/backGround.jpg")
```

- パス指定が正しく機能していないため、実際の配置場所に合わせて修正する。

また、以下は単位が指定されていない。

```css
margin-bottom: 2.5;
```

例えば、意図する値に応じて以下のように単位を指定する。

```css
margin-bottom: 2.5rem;
```

## 優先度まとめ

| 優先度 | 内容                           | 対応方針                                      |
| ------ | ------------------------------ | --------------------------------------------- |
| 大     | エラー情報の露出               | サーバー側ログへ移行                          |
| 大     | いいね機能の脆弱性             | バックエンドで検証・制限                      |
| 大     | 画像サイズ過大                 | サムネイル化・WebP化など                      |
| 大     | クラス名の重複・誤り           | 正しいクラス名へ修正                          |
| 大     | GamePageControllerの取得先誤り | `getGameFilePath` 等を確認・修正              |
| 大     | HTML構造の崩れ                 | タグ構造を修正・Validatorで確認               |
| 大     | `.gitignore` の齟齬            | 認証情報のコミット防止を最優先で対応          |
| 中     | 全ファイル走査                 | データ増加を想定した取得方式へ改善            |
| 中     | DB取得処理                     | `COUNT`・必要カラム取得・インデックス等を検討 |
| 中     | ルート判定の部分一致           | 完全一致・正規表現などへ変更                  |
| 中     | 命名規則・タイポ               | 命名ルールを統一                              |
| 小     | README・description            | 提出・第三者閲覧を意識して充実させる          |
| 小     | CSSの不備                      | パス・単位指定を修正                          |
